# Copy to .env (never commit .env).

# Use EXPO_PUBLIC_ so Expo bundles these at build time.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=

# Optional — local UI preview only. Requires Metro __DEV__. Never set in production / EAS prod.
# EXPO_PUBLIC_DEV_LOGIN_BYPASS=true