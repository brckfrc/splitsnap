legacy-peer-deps=true

; Turns the `engines.node` range into a hard error instead of a warning, so an
; install on the wrong Node version fails here rather than producing a lockfile or
; native modules that do not match what .nvmrc, Xcode and EAS Build all use.
engine-strict=true
